import { ThemeColorPath } from '@/theme/color';

export type Props = {
  orientation?: 'horizontal' | 'vertical';
  thickness?: string;
  length?: string;
  color?: ThemeColorPath;
  margin?: string;
};
